# Spring boot microservices using keycloak

## Keycloak

### Start keycloak

```bash
docker-compose up -d
```

## Spring boot

### Start spring boot

For eache service, run the following command:

```bash
./gradlew bootRun
```
